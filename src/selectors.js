const customTagListEl = document.getElementById('custom-tags')
const taskListEl = document.getElementById('task-list')
const newTagButtonEl = document.getElementById('new-tag')
const newTodoButtonEl = document.getElementById('new-task')
const dialogEl = document.getElementById('dialog-tag')
const dialogEl2 = document.getElementById('dialog-task')
const dialogInputEl = document.getElementById('dialog-tag-input')
const dialogInputEl2 = document.getElementById('dialog-task-input')
const dialogButtonEl = document.getElementById('tag-button')
const dialogButtonEl2 = document.getElementById('task-button')
const dialogSelectEl = document.getElementById('dialog-task-select')
const dialogCancelEl = document.getElementById('dialog-tag-cancel')
const dialogCancelEl2 = document.getElementById('dialog-cancel-task')
const dialogSelectedEl = document.getElementById('dialog-task-selected')
const tagAllEl = document.getElementById('tag-all')
const tagImportantEl = document.getElementById('tag-important')

export {
    customTagListEl,
    taskListEl,
    newTagButtonEl,
    newTodoButtonEl,
    dialogEl,
    dialogEl2,
    dialogInputEl,
    dialogInputEl2,
    dialogButtonEl,
    dialogButtonEl2,
    dialogSelectEl,
    dialogCancelEl,
    dialogCancelEl2,
    dialogSelectedEl,
    tagAllEl,
    tagImportantEl
}
