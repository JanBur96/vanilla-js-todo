const customTagListEl = document.getElementById('custom-tags')
const taskListEl = document.getElementById('task-list')
const newTagButtonEl = document.getElementById('new-tag')
const newTaskButtonEl = document.getElementById('new-task')
const dialogTagEl = document.getElementById('dialog-tag')
const dialogTaskEl = document.getElementById('dialog-task')
const dialogInputTagEl = document.getElementById('dialog-tag-input')
const dialogInputTaskEl = document.getElementById('dialog-task-input')
const newTagDialogButtonEl = document.getElementById('tag-button')
const newTaskDialogButtonEl = document.getElementById('task-button')
const newTaskDialogSelectEl = document.getElementById('dialog-task-select')
const newTagDialogCancelEl = document.getElementById('dialog-tag-cancel')
const newTaskDialogCancelEl = document.getElementById('dialog-cancel-task')
const dialogSelectedEl = document.getElementById('dialog-task-selected')
const tagAllEl = document.getElementById('tag-all')
const tagImportantEl = document.getElementById('tag-important')

export {
    customTagListEl,
    taskListEl,
    newTagButtonEl,
    newTaskButtonEl,
    dialogTagEl,
    dialogTaskEl,
    dialogInputTagEl,
    dialogInputTaskEl,
    newTagDialogButtonEl,
    newTaskDialogButtonEl,
    newTaskDialogSelectEl,
    newTagDialogCancelEl,
    newTaskDialogCancelEl,
    dialogSelectedEl,
    tagAllEl,
    tagImportantEl,
}
