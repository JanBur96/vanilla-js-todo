const customTagListEl = document.getElementById('custom-tags')
const taskListEl = document.getElementById('task-list')
const newTagButtonEl = document.getElementById('new-tag')
const newTodoButtonEl = document.getElementById('new-task')
const dialogTagEl = document.getElementById('dialog-tag')
const dialogTaskEl = document.getElementById('dialog-task')
const dialogInputTagEl = document.getElementById('dialog-tag-input')
const dialogInputTaskEl = document.getElementById('dialog-task-input')
const dialogButtonTagEl = document.getElementById('tag-button')
const dialogButtonTaskEl = document.getElementById('task-button')
const dialogSelectEl = document.getElementById('dialog-task-select')
const dialogCancelTagEl = document.getElementById('dialog-tag-cancel')
const dialogCancelTaskEl = document.getElementById('dialog-cancel-task')
const dialogSelectedEl = document.getElementById('dialog-task-selected')
const tagAllEl = document.getElementById('tag-all')
const tagImportantEl = document.getElementById('tag-important')
const finishTodoEl = document.getElementById('finish-todo')

export {
    customTagListEl,
    taskListEl,
    newTagButtonEl,
    newTodoButtonEl,
    dialogTagEl,
    dialogTaskEl,
    dialogInputTagEl,
    dialogInputTaskEl,
    dialogButtonTagEl,
    dialogButtonTaskEl,
    dialogSelectEl,
    dialogCancelTagEl,
    dialogCancelTaskEl,
    dialogSelectedEl,
    tagAllEl,
    tagImportantEl,
    finishTodoEl
}
